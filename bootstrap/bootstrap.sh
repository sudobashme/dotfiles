#!/bin/sh
# Legacy bootstrap script — archived.
# Prefer: deploy.zsh or lamina (bin/lamina deploy) for current Personal OS Layer setup.
#
# This file predates ZDOTDIR, lamina, dotter, and starship. Kept as historical reference only.

set -eu

echo "bootstrap.sh is deprecated. Use deploy.zsh or lamina instead." >&2
exit 1