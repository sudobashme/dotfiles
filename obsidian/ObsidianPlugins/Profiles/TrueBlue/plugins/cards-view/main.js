/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __publicField = (obj, key, value) => {
  __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
  return value;
};

// main.ts
var main_exports = {};
__export(main_exports, {
  default: () => CardsViewPlugin2
});
module.exports = __toCommonJS(main_exports);
var import_obsidian6 = require("obsidian");

// settings.ts
var import_obsidian = require("obsidian");
var DEFAULT_SETTINGS = {
  minCardWidth: 200,
  launchOnStart: false,
  displayTitle: "Both" /* Both */,
  pinnedFiles: []
};
var CardsViewSettingsTab = class extends import_obsidian.PluginSettingTab {
  constructor(app2, plugin) {
    super(app2, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian.Setting(containerEl).setName("Minimum card width").setDesc("Cards will not be smaller than this width (in pixels)").addText(
      (text2) => text2.setPlaceholder("200").setValue(this.plugin.settings.minCardWidth.toString()).onChange(async (value) => {
        if (isNaN(parseInt(value))) {
          new import_obsidian.Notice("Invalid number");
          return;
        }
        this.plugin.settings.minCardWidth = parseInt(value);
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Title display mode").setDesc("What to display on cards starting with a # Level 1 title").addDropdown(
      (dropdown) => dropdown.addOptions({
        ["Both" /* Both */]: "Both title and filename",
        ["Title" /* Title */]: "Title",
        ["Filename" /* Filename */]: "Filename"
      }).setValue(this.plugin.settings.displayTitle).onChange(async (value) => {
        this.plugin.settings.displayTitle = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Launch on start").setDesc("Open the cards view when Obsidian starts").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.launchOnStart).onChange(async (value) => {
        this.plugin.settings.launchOnStart = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Reset settings").setDesc("Reset all settings to default").addButton(
      (button) => button.setButtonText("Reset").onClick(async () => {
        this.plugin.settings = DEFAULT_SETTINGS;
        await this.plugin.saveSettings();
        this.display();
      })
    );
  }
};

// view.ts
var import_obsidian5 = require("obsidian");

// node_modules/svelte/src/runtime/internal/utils.js
function noop() {
}
function run(fn) {
  return fn();
}
function blank_object() {
  return /* @__PURE__ */ Object.create(null);
}
function run_all(fns) {
  fns.forEach(run);
}
function is_function(thing) {
  return typeof thing === "function";
}
function safe_not_equal(a, b) {
  return a != a ? b == b : a !== b || a && typeof a === "object" || typeof a === "function";
}
function is_empty(obj) {
  return Object.keys(obj).length === 0;
}
function subscribe(store, ...callbacks) {
  if (store == null) {
    for (const callback of callbacks) {
      callback(void 0);
    }
    return noop;
  }
  const unsub = store.subscribe(...callbacks);
  return unsub.unsubscribe ? () => unsub.unsubscribe() : unsub;
}
function get_store_value(store) {
  let value;
  subscribe(store, (_) => value = _)();
  return value;
}
function component_subscribe(component, store, callback) {
  component.$$.on_destroy.push(subscribe(store, callback));
}
function set_store_value(store, ret, value) {
  store.set(value);
  return ret;
}
function action_destroyer(action_result) {
  return action_result && is_function(action_result.destroy) ? action_result.destroy : noop;
}

// node_modules/svelte/src/runtime/internal/globals.js
var globals = typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : (
  // @ts-ignore Node typings have this
  global
);

// node_modules/svelte/src/runtime/internal/ResizeObserverSingleton.js
var ResizeObserverSingleton = class _ResizeObserverSingleton {
  /** @param {ResizeObserverOptions} options */
  constructor(options) {
    /**
     * @private
     * @readonly
     * @type {WeakMap<Element, import('./private.js').Listener>}
     */
    __publicField(this, "_listeners", "WeakMap" in globals ? /* @__PURE__ */ new WeakMap() : void 0);
    /**
     * @private
     * @type {ResizeObserver}
     */
    __publicField(this, "_observer");
    /** @type {ResizeObserverOptions} */
    __publicField(this, "options");
    this.options = options;
  }
  /**
   * @param {Element} element
   * @param {import('./private.js').Listener} listener
   * @returns {() => void}
   */
  observe(element2, listener) {
    this._listeners.set(element2, listener);
    this._getObserver().observe(element2, this.options);
    return () => {
      this._listeners.delete(element2);
      this._observer.unobserve(element2);
    };
  }
  /**
   * @private
   */
  _getObserver() {
    var _a;
    return (_a = this._observer) != null ? _a : this._observer = new ResizeObserver((entries) => {
      var _a2;
      for (const entry of entries) {
        _ResizeObserverSingleton.entries.set(entry.target, entry);
        (_a2 = this._listeners.get(entry.target)) == null ? void 0 : _a2(entry);
      }
    });
  }
};
ResizeObserverSingleton.entries = "WeakMap" in globals ? /* @__PURE__ */ new WeakMap() : void 0;

// node_modules/svelte/src/runtime/internal/dom.js
var is_hydrating = false;
function start_hydrating() {
  is_hydrating = true;
}
function end_hydrating() {
  is_hydrating = false;
}
function append(target, node) {
  target.appendChild(node);
}
function append_styles(target, style_sheet_id, styles) {
  const append_styles_to = get_root_for_style(target);
  if (!append_styles_to.getElementById(style_sheet_id)) {
    const style = element("style");
    style.id = style_sheet_id;
    style.textContent = styles;
    append_stylesheet(append_styles_to, style);
  }
}
function get_root_for_style(node) {
  if (!node)
    return document;
  const root = node.getRootNode ? node.getRootNode() : node.ownerDocument;
  if (root && /** @type {ShadowRoot} */
  root.host) {
    return (
      /** @type {ShadowRoot} */
      root
    );
  }
  return node.ownerDocument;
}
function append_stylesheet(node, style) {
  append(
    /** @type {Document} */
    node.head || node,
    style
  );
  return style.sheet;
}
function insert(target, node, anchor) {
  target.insertBefore(node, anchor || null);
}
function detach(node) {
  if (node.parentNode) {
    node.parentNode.removeChild(node);
  }
}
function destroy_each(iterations, detaching) {
  for (let i = 0; i < iterations.length; i += 1) {
    if (iterations[i])
      iterations[i].d(detaching);
  }
}
function element(name) {
  return document.createElement(name);
}
function text(data) {
  return document.createTextNode(data);
}
function space() {
  return text(" ");
}
function empty() {
  return text("");
}
function listen(node, event, handler, options) {
  node.addEventListener(event, handler, options);
  return () => node.removeEventListener(event, handler, options);
}
function stop_propagation(fn) {
  return function(event) {
    event.stopPropagation();
    return fn.call(this, event);
  };
}
function attr(node, attribute, value) {
  if (value == null)
    node.removeAttribute(attribute);
  else if (node.getAttribute(attribute) !== value)
    node.setAttribute(attribute, value);
}
function children(element2) {
  return Array.from(element2.childNodes);
}
function set_data(text2, data) {
  data = "" + data;
  if (text2.data === data)
    return;
  text2.data = /** @type {string} */
  data;
}
function set_style(node, key, value, important) {
  if (value == null) {
    node.style.removeProperty(key);
  } else {
    node.style.setProperty(key, value, important ? "important" : "");
  }
}
function toggle_class(element2, name, toggle) {
  element2.classList.toggle(name, !!toggle);
}
function custom_event(type, detail, { bubbles = false, cancelable = false } = {}) {
  return new CustomEvent(type, { detail, bubbles, cancelable });
}
function get_custom_elements_slots(element2) {
  const result = {};
  element2.childNodes.forEach(
    /** @param {Element} node */
    (node) => {
      result[node.slot || "default"] = true;
    }
  );
  return result;
}

// node_modules/svelte/src/runtime/internal/lifecycle.js
var current_component;
function set_current_component(component) {
  current_component = component;
}
function get_current_component() {
  if (!current_component)
    throw new Error("Function called outside component initialization");
  return current_component;
}
function onMount(fn) {
  get_current_component().$$.on_mount.push(fn);
}
function afterUpdate(fn) {
  get_current_component().$$.after_update.push(fn);
}
function createEventDispatcher() {
  const component = get_current_component();
  return (type, detail, { cancelable = false } = {}) => {
    const callbacks = component.$$.callbacks[type];
    if (callbacks) {
      const event = custom_event(
        /** @type {string} */
        type,
        detail,
        { cancelable }
      );
      callbacks.slice().forEach((fn) => {
        fn.call(component, event);
      });
      return !event.defaultPrevented;
    }
    return true;
  };
}

// node_modules/svelte/src/runtime/internal/scheduler.js
var dirty_components = [];
var binding_callbacks = [];
var render_callbacks = [];
var flush_callbacks = [];
var resolved_promise = /* @__PURE__ */ Promise.resolve();
var update_scheduled = false;
function schedule_update() {
  if (!update_scheduled) {
    update_scheduled = true;
    resolved_promise.then(flush);
  }
}
function add_render_callback(fn) {
  render_callbacks.push(fn);
}
var seen_callbacks = /* @__PURE__ */ new Set();
var flushidx = 0;
function flush() {
  if (flushidx !== 0) {
    return;
  }
  const saved_component = current_component;
  do {
    try {
      while (flushidx < dirty_components.length) {
        const component = dirty_components[flushidx];
        flushidx++;
        set_current_component(component);
        update(component.$$);
      }
    } catch (e) {
      dirty_components.length = 0;
      flushidx = 0;
      throw e;
    }
    set_current_component(null);
    dirty_components.length = 0;
    flushidx = 0;
    while (binding_callbacks.length)
      binding_callbacks.pop()();
    for (let i = 0; i < render_callbacks.length; i += 1) {
      const callback = render_callbacks[i];
      if (!seen_callbacks.has(callback)) {
        seen_callbacks.add(callback);
        callback();
      }
    }
    render_callbacks.length = 0;
  } while (dirty_components.length);
  while (flush_callbacks.length) {
    flush_callbacks.pop()();
  }
  update_scheduled = false;
  seen_callbacks.clear();
  set_current_component(saved_component);
}
function update($$) {
  if ($$.fragment !== null) {
    $$.update();
    run_all($$.before_update);
    const dirty = $$.dirty;
    $$.dirty = [-1];
    $$.fragment && $$.fragment.p($$.ctx, dirty);
    $$.after_update.forEach(add_render_callback);
  }
}
function flush_render_callbacks(fns) {
  const filtered = [];
  const targets = [];
  render_callbacks.forEach((c) => fns.indexOf(c) === -1 ? filtered.push(c) : targets.push(c));
  targets.forEach((c) => c());
  render_callbacks = filtered;
}

// node_modules/svelte/src/runtime/internal/transitions.js
var outroing = /* @__PURE__ */ new Set();
var outros;
function group_outros() {
  outros = {
    r: 0,
    c: [],
    p: outros
    // parent group
  };
}
function check_outros() {
  if (!outros.r) {
    run_all(outros.c);
  }
  outros = outros.p;
}
function transition_in(block, local) {
  if (block && block.i) {
    outroing.delete(block);
    block.i(local);
  }
}
function transition_out(block, local, detach2, callback) {
  if (block && block.o) {
    if (outroing.has(block))
      return;
    outroing.add(block);
    outros.c.push(() => {
      outroing.delete(block);
      if (callback) {
        if (detach2)
          block.d(1);
        callback();
      }
    });
    block.o(local);
  } else if (callback) {
    callback();
  }
}

// node_modules/svelte/src/runtime/internal/each.js
function ensure_array_like(array_like_or_iterator) {
  return (array_like_or_iterator == null ? void 0 : array_like_or_iterator.length) !== void 0 ? array_like_or_iterator : Array.from(array_like_or_iterator);
}
function outro_and_destroy_block(block, lookup) {
  transition_out(block, 1, 1, () => {
    lookup.delete(block.key);
  });
}
function update_keyed_each(old_blocks, dirty, get_key, dynamic, ctx, list, lookup, node, destroy, create_each_block2, next, get_context) {
  let o = old_blocks.length;
  let n = list.length;
  let i = o;
  const old_indexes = {};
  while (i--)
    old_indexes[old_blocks[i].key] = i;
  const new_blocks = [];
  const new_lookup = /* @__PURE__ */ new Map();
  const deltas = /* @__PURE__ */ new Map();
  const updates = [];
  i = n;
  while (i--) {
    const child_ctx = get_context(ctx, list, i);
    const key = get_key(child_ctx);
    let block = lookup.get(key);
    if (!block) {
      block = create_each_block2(key, child_ctx);
      block.c();
    } else if (dynamic) {
      updates.push(() => block.p(child_ctx, dirty));
    }
    new_lookup.set(key, new_blocks[i] = block);
    if (key in old_indexes)
      deltas.set(key, Math.abs(i - old_indexes[key]));
  }
  const will_move = /* @__PURE__ */ new Set();
  const did_move = /* @__PURE__ */ new Set();
  function insert2(block) {
    transition_in(block, 1);
    block.m(node, next);
    lookup.set(block.key, block);
    next = block.first;
    n--;
  }
  while (o && n) {
    const new_block = new_blocks[n - 1];
    const old_block = old_blocks[o - 1];
    const new_key = new_block.key;
    const old_key = old_block.key;
    if (new_block === old_block) {
      next = new_block.first;
      o--;
      n--;
    } else if (!new_lookup.has(old_key)) {
      destroy(old_block, lookup);
      o--;
    } else if (!lookup.has(new_key) || will_move.has(new_key)) {
      insert2(new_block);
    } else if (did_move.has(old_key)) {
      o--;
    } else if (deltas.get(new_key) > deltas.get(old_key)) {
      did_move.add(new_key);
      insert2(new_block);
    } else {
      will_move.add(old_key);
      o--;
    }
  }
  while (o--) {
    const old_block = old_blocks[o];
    if (!new_lookup.has(old_block.key))
      destroy(old_block, lookup);
  }
  while (n)
    insert2(new_blocks[n - 1]);
  run_all(updates);
  return new_blocks;
}

// node_modules/svelte/src/shared/boolean_attributes.js
var _boolean_attributes = (
  /** @type {const} */
  [
    "allowfullscreen",
    "allowpaymentrequest",
    "async",
    "autofocus",
    "autoplay",
    "checked",
    "controls",
    "default",
    "defer",
    "disabled",
    "formnovalidate",
    "hidden",
    "inert",
    "ismap",
    "loop",
    "multiple",
    "muted",
    "nomodule",
    "novalidate",
    "open",
    "playsinline",
    "readonly",
    "required",
    "reversed",
    "selected"
  ]
);
var boolean_attributes = /* @__PURE__ */ new Set([..._boolean_attributes]);

// node_modules/svelte/src/runtime/internal/Component.js
function create_component(block) {
  block && block.c();
}
function mount_component(component, target, anchor) {
  const { fragment, after_update } = component.$$;
  fragment && fragment.m(target, anchor);
  add_render_callback(() => {
    const new_on_destroy = component.$$.on_mount.map(run).filter(is_function);
    if (component.$$.on_destroy) {
      component.$$.on_destroy.push(...new_on_destroy);
    } else {
      run_all(new_on_destroy);
    }
    component.$$.on_mount = [];
  });
  after_update.forEach(add_render_callback);
}
function destroy_component(component, detaching) {
  const $$ = component.$$;
  if ($$.fragment !== null) {
    flush_render_callbacks($$.after_update);
    run_all($$.on_destroy);
    $$.fragment && $$.fragment.d(detaching);
    $$.on_destroy = $$.fragment = null;
    $$.ctx = [];
  }
}
function make_dirty(component, i) {
  if (component.$$.dirty[0] === -1) {
    dirty_components.push(component);
    schedule_update();
    component.$$.dirty.fill(0);
  }
  component.$$.dirty[i / 31 | 0] |= 1 << i % 31;
}
function init(component, options, instance3, create_fragment3, not_equal, props, append_styles2 = null, dirty = [-1]) {
  const parent_component = current_component;
  set_current_component(component);
  const $$ = component.$$ = {
    fragment: null,
    ctx: [],
    // state
    props,
    update: noop,
    not_equal,
    bound: blank_object(),
    // lifecycle
    on_mount: [],
    on_destroy: [],
    on_disconnect: [],
    before_update: [],
    after_update: [],
    context: new Map(options.context || (parent_component ? parent_component.$$.context : [])),
    // everything else
    callbacks: blank_object(),
    dirty,
    skip_bound: false,
    root: options.target || parent_component.$$.root
  };
  append_styles2 && append_styles2($$.root);
  let ready = false;
  $$.ctx = instance3 ? instance3(component, options.props || {}, (i, ret, ...rest) => {
    const value = rest.length ? rest[0] : ret;
    if ($$.ctx && not_equal($$.ctx[i], $$.ctx[i] = value)) {
      if (!$$.skip_bound && $$.bound[i])
        $$.bound[i](value);
      if (ready)
        make_dirty(component, i);
    }
    return ret;
  }) : [];
  $$.update();
  ready = true;
  run_all($$.before_update);
  $$.fragment = create_fragment3 ? create_fragment3($$.ctx) : false;
  if (options.target) {
    if (options.hydrate) {
      start_hydrating();
      const nodes = children(options.target);
      $$.fragment && $$.fragment.l(nodes);
      nodes.forEach(detach);
    } else {
      $$.fragment && $$.fragment.c();
    }
    if (options.intro)
      transition_in(component.$$.fragment);
    mount_component(component, options.target, options.anchor);
    end_hydrating();
    flush();
  }
  set_current_component(parent_component);
}
var SvelteElement;
if (typeof HTMLElement === "function") {
  SvelteElement = class extends HTMLElement {
    constructor($$componentCtor, $$slots, use_shadow_dom) {
      super();
      /** The Svelte component constructor */
      __publicField(this, "$$ctor");
      /** Slots */
      __publicField(this, "$$s");
      /** The Svelte component instance */
      __publicField(this, "$$c");
      /** Whether or not the custom element is connected */
      __publicField(this, "$$cn", false);
      /** Component props data */
      __publicField(this, "$$d", {});
      /** `true` if currently in the process of reflecting component props back to attributes */
      __publicField(this, "$$r", false);
      /** @type {Record<string, CustomElementPropDefinition>} Props definition (name, reflected, type etc) */
      __publicField(this, "$$p_d", {});
      /** @type {Record<string, Function[]>} Event listeners */
      __publicField(this, "$$l", {});
      /** @type {Map<Function, Function>} Event listener unsubscribe functions */
      __publicField(this, "$$l_u", /* @__PURE__ */ new Map());
      this.$$ctor = $$componentCtor;
      this.$$s = $$slots;
      if (use_shadow_dom) {
        this.attachShadow({ mode: "open" });
      }
    }
    addEventListener(type, listener, options) {
      this.$$l[type] = this.$$l[type] || [];
      this.$$l[type].push(listener);
      if (this.$$c) {
        const unsub = this.$$c.$on(type, listener);
        this.$$l_u.set(listener, unsub);
      }
      super.addEventListener(type, listener, options);
    }
    removeEventListener(type, listener, options) {
      super.removeEventListener(type, listener, options);
      if (this.$$c) {
        const unsub = this.$$l_u.get(listener);
        if (unsub) {
          unsub();
          this.$$l_u.delete(listener);
        }
      }
    }
    async connectedCallback() {
      this.$$cn = true;
      if (!this.$$c) {
        let create_slot = function(name) {
          return () => {
            let node;
            const obj = {
              c: function create() {
                node = element("slot");
                if (name !== "default") {
                  attr(node, "name", name);
                }
              },
              /**
               * @param {HTMLElement} target
               * @param {HTMLElement} [anchor]
               */
              m: function mount(target, anchor) {
                insert(target, node, anchor);
              },
              d: function destroy(detaching) {
                if (detaching) {
                  detach(node);
                }
              }
            };
            return obj;
          };
        };
        await Promise.resolve();
        if (!this.$$cn || this.$$c) {
          return;
        }
        const $$slots = {};
        const existing_slots = get_custom_elements_slots(this);
        for (const name of this.$$s) {
          if (name in existing_slots) {
            $$slots[name] = [create_slot(name)];
          }
        }
        for (const attribute of this.attributes) {
          const name = this.$$g_p(attribute.name);
          if (!(name in this.$$d)) {
            this.$$d[name] = get_custom_element_value(name, attribute.value, this.$$p_d, "toProp");
          }
        }
        for (const key in this.$$p_d) {
          if (!(key in this.$$d) && this[key] !== void 0) {
            this.$$d[key] = this[key];
            delete this[key];
          }
        }
        this.$$c = new this.$$ctor({
          target: this.shadowRoot || this,
          props: {
            ...this.$$d,
            $$slots,
            $$scope: {
              ctx: []
            }
          }
        });
        const reflect_attributes = () => {
          this.$$r = true;
          for (const key in this.$$p_d) {
            this.$$d[key] = this.$$c.$$.ctx[this.$$c.$$.props[key]];
            if (this.$$p_d[key].reflect) {
              const attribute_value = get_custom_element_value(
                key,
                this.$$d[key],
                this.$$p_d,
                "toAttribute"
              );
              if (attribute_value == null) {
                this.removeAttribute(this.$$p_d[key].attribute || key);
              } else {
                this.setAttribute(this.$$p_d[key].attribute || key, attribute_value);
              }
            }
          }
          this.$$r = false;
        };
        this.$$c.$$.after_update.push(reflect_attributes);
        reflect_attributes();
        for (const type in this.$$l) {
          for (const listener of this.$$l[type]) {
            const unsub = this.$$c.$on(type, listener);
            this.$$l_u.set(listener, unsub);
          }
        }
        this.$$l = {};
      }
    }
    // We don't need this when working within Svelte code, but for compatibility of people using this outside of Svelte
    // and setting attributes through setAttribute etc, this is helpful
    attributeChangedCallback(attr2, _oldValue, newValue) {
      var _a;
      if (this.$$r)
        return;
      attr2 = this.$$g_p(attr2);
      this.$$d[attr2] = get_custom_element_value(attr2, newValue, this.$$p_d, "toProp");
      (_a = this.$$c) == null ? void 0 : _a.$set({ [attr2]: this.$$d[attr2] });
    }
    disconnectedCallback() {
      this.$$cn = false;
      Promise.resolve().then(() => {
        if (!this.$$cn && this.$$c) {
          this.$$c.$destroy();
          this.$$c = void 0;
        }
      });
    }
    $$g_p(attribute_name) {
      return Object.keys(this.$$p_d).find(
        (key) => this.$$p_d[key].attribute === attribute_name || !this.$$p_d[key].attribute && key.toLowerCase() === attribute_name
      ) || attribute_name;
    }
  };
}
function get_custom_element_value(prop, value, props_definition, transform) {
  var _a;
  const type = (_a = props_definition[prop]) == null ? void 0 : _a.type;
  value = type === "Boolean" && typeof value !== "boolean" ? value != null : value;
  if (!transform || !props_definition[prop]) {
    return value;
  } else if (transform === "toAttribute") {
    switch (type) {
      case "Object":
      case "Array":
        return value == null ? null : JSON.stringify(value);
      case "Boolean":
        return value ? "" : null;
      case "Number":
        return value == null ? null : value;
      default:
        return value;
    }
  } else {
    switch (type) {
      case "Object":
      case "Array":
        return value && JSON.parse(value);
      case "Boolean":
        return value;
      case "Number":
        return value != null ? +value : value;
      default:
        return value;
    }
  }
}
var SvelteComponent = class {
  constructor() {
    /**
     * ### PRIVATE API
     *
     * Do not use, may change at any time
     *
     * @type {any}
     */
    __publicField(this, "$$");
    /**
     * ### PRIVATE API
     *
     * Do not use, may change at any time
     *
     * @type {any}
     */
    __publicField(this, "$$set");
  }
  /** @returns {void} */
  $destroy() {
    destroy_component(this, 1);
    this.$destroy = noop;
  }
  /**
   * @template {Extract<keyof Events, string>} K
   * @param {K} type
   * @param {((e: Events[K]) => void) | null | undefined} callback
   * @returns {() => void}
   */
  $on(type, callback) {
    if (!is_function(callback)) {
      return noop;
    }
    const callbacks = this.$$.callbacks[type] || (this.$$.callbacks[type] = []);
    callbacks.push(callback);
    return () => {
      const index = callbacks.indexOf(callback);
      if (index !== -1)
        callbacks.splice(index, 1);
    };
  }
  /**
   * @param {Partial<Props>} props
   * @returns {void}
   */
  $set(props) {
    if (this.$$set && !is_empty(props)) {
      this.$$.skip_bound = true;
      this.$$set(props);
      this.$$.skip_bound = false;
    }
  }
};

// node_modules/svelte/src/shared/version.js
var PUBLIC_VERSION = "4";

// node_modules/svelte/src/runtime/internal/disclose-version/index.js
if (typeof window !== "undefined")
  (window.__svelte || (window.__svelte = { v: /* @__PURE__ */ new Set() })).v.add(PUBLIC_VERSION);

// node_modules/tslib/tslib.es6.mjs
function __awaiter(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}

// components/Root.svelte
var import_obsidian4 = require("obsidian");

// node_modules/minimasonry/build/minimasonry.esm.js
var MiniMasonry = function(conf) {
  this._sizes = [];
  this._columns = [];
  this._container = null;
  this._count = null;
  this._width = 0;
  this._removeListener = null;
  this._currentGutterX = null;
  this._currentGutterY = null;
  this._resizeTimeout = null, this.conf = {
    baseWidth: 255,
    gutterX: null,
    gutterY: null,
    gutter: 10,
    container: null,
    minify: true,
    ultimateGutter: 5,
    surroundingGutter: true,
    direction: "ltr",
    wedge: false
  };
  this.init(conf);
  return this;
};
MiniMasonry.prototype.init = function(conf) {
  for (var i in this.conf) {
    if (conf[i] != void 0) {
      this.conf[i] = conf[i];
    }
  }
  if (this.conf.gutterX == null || this.conf.gutterY == null) {
    this.conf.gutterX = this.conf.gutterY = this.conf.gutter;
  }
  this._currentGutterX = this.conf.gutterX;
  this._currentGutterY = this.conf.gutterY;
  this._container = typeof this.conf.container == "object" && this.conf.container.nodeName ? this.conf.container : document.querySelector(this.conf.container);
  if (!this._container) {
    throw new Error("Container not found or missing");
  }
  var onResize = this.resizeThrottler.bind(this);
  window.addEventListener("resize", onResize);
  this._removeListener = function() {
    window.removeEventListener("resize", onResize);
    if (this._resizeTimeout != null) {
      window.clearTimeout(this._resizeTimeout);
      this._resizeTimeout = null;
    }
  };
  this.layout();
};
MiniMasonry.prototype.reset = function() {
  this._sizes = [];
  this._columns = [];
  this._count = null;
  this._width = this._container.clientWidth;
  var minWidth = this.conf.baseWidth;
  if (this._width < minWidth) {
    this._width = minWidth;
    this._container.style.minWidth = minWidth + "px";
  }
  if (this.getCount() == 1) {
    this._currentGutterX = this.conf.ultimateGutter;
    this._count = 1;
  } else if (this._width < this.conf.baseWidth + 2 * this._currentGutterX) {
    this._currentGutterX = 0;
  } else {
    this._currentGutterX = this.conf.gutterX;
  }
};
MiniMasonry.prototype.getCount = function() {
  if (this.conf.surroundingGutter) {
    return Math.floor((this._width - this._currentGutterX) / (this.conf.baseWidth + this._currentGutterX));
  }
  return Math.floor((this._width + this._currentGutterX) / (this.conf.baseWidth + this._currentGutterX));
};
MiniMasonry.prototype.computeWidth = function() {
  var width;
  if (this.conf.surroundingGutter) {
    width = (this._width - this._currentGutterX) / this._count - this._currentGutterX;
  } else {
    width = (this._width + this._currentGutterX) / this._count - this._currentGutterX;
  }
  width = Number.parseFloat(width.toFixed(2));
  return width;
};
MiniMasonry.prototype.layout = function() {
  if (!this._container) {
    console.error("Container not found");
    return;
  }
  this.reset();
  if (this._count == null) {
    this._count = this.getCount();
  }
  var colWidth = this.computeWidth();
  for (var i = 0; i < this._count; i++) {
    this._columns[i] = 0;
  }
  var children2 = this._container.children;
  for (var k = 0; k < children2.length; k++) {
    children2[k].style.width = colWidth + "px";
    this._sizes[k] = children2[k].clientHeight;
  }
  var startX;
  if (this.conf.direction == "ltr") {
    startX = this.conf.surroundingGutter ? this._currentGutterX : 0;
  } else {
    startX = this._width - (this.conf.surroundingGutter ? this._currentGutterX : 0);
  }
  if (this._count > this._sizes.length) {
    var occupiedSpace = this._sizes.length * (colWidth + this._currentGutterX) - this._currentGutterX;
    if (this.conf.wedge === false) {
      if (this.conf.direction == "ltr") {
        startX = (this._width - occupiedSpace) / 2;
      } else {
        startX = this._width - (this._width - occupiedSpace) / 2;
      }
    } else {
      if (this.conf.direction == "ltr")
        ;
      else {
        startX = this._width - this._currentGutterX;
      }
    }
  }
  for (var index = 0; index < children2.length; index++) {
    var nextColumn = this.conf.minify ? this.getShortest() : this.getNextColumn(index);
    var childrenGutter = 0;
    if (this.conf.surroundingGutter || nextColumn != this._columns.length) {
      childrenGutter = this._currentGutterX;
    }
    var x;
    if (this.conf.direction == "ltr") {
      x = startX + (colWidth + childrenGutter) * nextColumn;
    } else {
      x = startX - (colWidth + childrenGutter) * nextColumn - colWidth;
    }
    var y = this._columns[nextColumn];
    children2[index].style.transform = "translate3d(" + Math.round(x) + "px," + Math.round(y) + "px,0)";
    this._columns[nextColumn] += this._sizes[index] + (this._count > 1 ? this.conf.gutterY : this.conf.ultimateGutter);
  }
  this._container.style.height = this._columns[this.getLongest()] - this._currentGutterY + "px";
};
MiniMasonry.prototype.getNextColumn = function(index) {
  return index % this._columns.length;
};
MiniMasonry.prototype.getShortest = function() {
  var shortest = 0;
  for (var i = 0; i < this._count; i++) {
    if (this._columns[i] < this._columns[shortest]) {
      shortest = i;
    }
  }
  return shortest;
};
MiniMasonry.prototype.getLongest = function() {
  var longest = 0;
  for (var i = 0; i < this._count; i++) {
    if (this._columns[i] > this._columns[longest]) {
      longest = i;
    }
  }
  return longest;
};
MiniMasonry.prototype.resizeThrottler = function() {
  if (!this._resizeTimeout) {
    this._resizeTimeout = setTimeout(function() {
      this._resizeTimeout = null;
      if (this._container.clientWidth != this._width) {
        this.layout();
      }
    }.bind(this), 33);
  }
};
MiniMasonry.prototype.destroy = function() {
  if (typeof this._removeListener == "function") {
    this._removeListener();
  }
  var children2 = this._container.children;
  for (var k = 0; k < children2.length; k++) {
    children2[k].style.removeProperty("width");
    children2[k].style.removeProperty("transform");
  }
  this._container.style.removeProperty("height");
  this._container.style.removeProperty("min-width");
};

// components/Card.svelte
var import_obsidian3 = require("obsidian");

// components/store.ts
var import_obsidian2 = require("obsidian");

// node_modules/svelte/src/runtime/store/index.js
var subscriber_queue = [];
function readable(value, start) {
  return {
    subscribe: writable(value, start).subscribe
  };
}
function writable(value, start = noop) {
  let stop;
  const subscribers = /* @__PURE__ */ new Set();
  function set(new_value) {
    if (safe_not_equal(value, new_value)) {
      value = new_value;
      if (stop) {
        const run_queue = !subscriber_queue.length;
        for (const subscriber of subscribers) {
          subscriber[1]();
          subscriber_queue.push(subscriber, value);
        }
        if (run_queue) {
          for (let i = 0; i < subscriber_queue.length; i += 2) {
            subscriber_queue[i][0](subscriber_queue[i + 1]);
          }
          subscriber_queue.length = 0;
        }
      }
    }
  }
  function update2(fn) {
    set(fn(value));
  }
  function subscribe2(run2, invalidate = noop) {
    const subscriber = [run2, invalidate];
    subscribers.add(subscriber);
    if (subscribers.size === 1) {
      stop = start(set, update2) || noop;
    }
    run2(value);
    return () => {
      subscribers.delete(subscriber);
      if (subscribers.size === 0 && stop) {
        stop();
        stop = null;
      }
    };
  }
  return { set, update: update2, subscribe: subscribe2 };
}
function derived(stores, fn, initial_value) {
  const single = !Array.isArray(stores);
  const stores_array = single ? [stores] : stores;
  if (!stores_array.every(Boolean)) {
    throw new Error("derived() expects stores as input, got a falsy value");
  }
  const auto = fn.length < 2;
  return readable(initial_value, (set, update2) => {
    let started = false;
    const values = [];
    let pending = 0;
    let cleanup = noop;
    const sync = () => {
      if (pending) {
        return;
      }
      cleanup();
      const result = fn(single ? values[0] : values, set, update2);
      if (auto) {
        set(result);
      } else {
        cleanup = is_function(result) ? result : noop;
      }
    };
    const unsubscribers = stores_array.map(
      (store, i) => subscribe(
        store,
        (value) => {
          values[i] = value;
          pending &= ~(1 << i);
          if (started) {
            sync();
          }
        },
        () => {
          pending |= 1 << i;
        }
      )
    );
    started = true;
    sync();
    return function stop() {
      run_all(unsubscribers);
      cleanup();
      started = false;
    };
  });
}

// components/store.ts
var app = writable();
var view = writable();
var settings = writable();
var appCache = writable();
var files = writable([]);
var sort = writable("mtime" /* Modified */);
var sortedFiles = derived(
  [sort, files, settings],
  ([$sort, $files, $settings]) => [...$files].sort(
    (a, b) => ($settings.pinnedFiles.includes(b.path) ? 1 : 0) - ($settings.pinnedFiles.includes(a.path) ? 1 : 0) || b.stat[$sort] - a.stat[$sort]
  ),
  []
);
var searchQuery = writable("");
var preparedSearch = derived(
  searchQuery,
  ($searchQuery) => $searchQuery ? (0, import_obsidian2.prepareFuzzySearch)($searchQuery) : null
);
var searchResultFiles = derived(
  [preparedSearch, sortedFiles, appCache],
  ([$preparedSearch, $sortedFiles, $appCache], set) => {
    if ($preparedSearch == null) {
      set($sortedFiles);
      return;
    }
    Promise.all(
      $sortedFiles.map(async (file) => {
        const content = await file.vault.cachedRead(file);
        const tags2 = (0, import_obsidian2.getAllTags)($appCache.getFileCache(file)) || [];
        return [
          $preparedSearch(content),
          $preparedSearch(file.name),
          $preparedSearch(`#${tags2.join(" #")}`)
        ];
      })
    ).then((searchResults) => {
      set(
        $sortedFiles.filter((file, index) => {
          const [contentMatch, nameMatch, tagsMatch] = searchResults[index];
          return contentMatch && contentMatch.score > -2 || nameMatch && nameMatch.score > -2 || tagsMatch && tagsMatch.score > -2;
        })
      );
    });
  },
  get_store_value(sortedFiles)
);
var displayedCount = writable(50);
var displayedFiles = derived(
  [searchResultFiles, displayedCount],
  ([$searchResultFiles, $displayedCount]) => $searchResultFiles.slice(0, $displayedCount)
);
var viewIsVisible = writable(false);
var skipNextTransition = writable(true);
var tags = derived(
  [displayedFiles, appCache],
  ([$displayedFiles, $appCache]) => {
    const tags2 = $displayedFiles.map(
      (file) => (0, import_obsidian2.getAllTags)($appCache.getFileCache(file)) || []
    ).flat();
    const tagCounts = tags2.reduce(
      (acc, tag) => {
        acc[tag] = (acc[tag] || 0) + 1;
        return acc;
      },
      {}
    );
    return Object.keys(tagCounts).sort((a, b) => tagCounts[b] - tagCounts[a]);
  }
);
var store_default = {
  files,
  sort,
  searchQuery,
  searchResultFiles,
  displayedCount,
  displayedFiles,
  viewIsVisible,
  skipNextTransition,
  tags,
  app,
  view,
  settings,
  appCache
};

// components/Card.svelte
function add_css(target) {
  append_styles(target, "svelte-liaymg", ".card.svelte-liaymg.svelte-liaymg{position:absolute;background-color:var(--background-primary-alt);border:1px solid var(--background-modifier-border);padding:var(--card-padding);word-wrap:break-word;overflow-y:hidden;margin:0;transition-property:transform;transition-duration:0.4s;transform:translate(0, 100vh)}.card.skip-transition.svelte-liaymg.svelte-liaymg{transition:none}.card.svelte-liaymg.svelte-liaymg{font-size:0.8rem}.card.svelte-liaymg p,.card.svelte-liaymg ul{margin:0.3rem 0}.card.svelte-liaymg h1,.card.svelte-liaymg h2,.card.svelte-liaymg h3{margin:0 0 0.3rem}.card.svelte-liaymg ul{padding-left:var(--size-4-5)}.card.svelte-liaymg p:has(> span.image-embed):not(:has(br)){margin:0}.card.svelte-liaymg span.image-embed{margin:0 calc(-1 * var(--card-padding));width:calc(100% + 2 * var(--card-padding))}.card.svelte-liaymg p:has(> span.image-embed):not(:has(br)) span.image-embed{display:block;& img {\n      display: block;\n    }}.card.svelte-liaymg p:has(> span.image-embed):has(br) span.image-embed{display:inline-block}.card.svelte-liaymg p:has(> span.markdown-embed),.card.svelte-liaymg .block-language-dataview{overflow:hidden;max-height:5rem;position:relative}.card.svelte-liaymg p:has(> span.markdown-embed) > .embed-shadow,.card.svelte-liaymg .block-language-dataview > .embed-shadow{position:absolute;top:0;left:0;right:0;bottom:0;box-shadow:inset 0 -2rem 1rem -1rem var(--background-primary-alt)}.card.svelte-liaymg.svelte-liaymg:hover{border-color:var(--background-modifier-border-hover)}.card.svelte-liaymg .card-info.svelte-liaymg{margin:calc(-1 * var(--card-padding));margin-top:0;border-top:1px solid var(--background-modifier-border);padding:var(--size-4-1) var(--card-padding);background-color:var(--background-primary);display:flex;flex-direction:row;align-items:center;justify-content:space-between;gap:var(--size-4-1)}.card.svelte-liaymg .card-info .clickable-icon.is-active.svelte-liaymg{color:var(--tab-text-color-focused-active);background-color:var(--background-modifier-hover)}.card.svelte-liaymg .card-info .folder-name.svelte-liaymg{flex-grow:1;display:flex;align-items:center;justify-content:center;gap:var(--size-4-1);color:var(--text-muted)}");
}
function create_if_block_1(ctx) {
  let h1;
  let t_value = (
    /*file*/
    ctx[0].basename + ""
  );
  let t;
  return {
    c() {
      h1 = element("h1");
      t = text(t_value);
    },
    m(target, anchor) {
      insert(target, h1, anchor);
      append(h1, t);
    },
    p(ctx2, dirty) {
      if (dirty & /*file*/
      1 && t_value !== (t_value = /*file*/
      ctx2[0].basename + ""))
        set_data(t, t_value);
    },
    d(detaching) {
      if (detaching) {
        detach(h1);
      }
    }
  };
}
function create_if_block(ctx) {
  let div;
  let span;
  let folderIcon_action;
  let t_value = (
    /*file*/
    ctx[0].parent.path + ""
  );
  let t;
  let mounted;
  let dispose;
  return {
    c() {
      div = element("div");
      span = element("span");
      t = text(t_value);
      attr(div, "class", "folder-name svelte-liaymg");
    },
    m(target, anchor) {
      insert(target, div, anchor);
      append(div, span);
      append(div, t);
      if (!mounted) {
        dispose = action_destroyer(folderIcon_action = /*folderIcon*/
        ctx[10].call(null, span));
        mounted = true;
      }
    },
    p(ctx2, dirty) {
      if (dirty & /*file*/
      1 && t_value !== (t_value = /*file*/
      ctx2[0].parent.path + ""))
        set_data(t, t_value);
    },
    d(detaching) {
      if (detaching) {
        detach(div);
      }
      mounted = false;
      dispose();
    }
  };
}
function create_fragment(ctx) {
  let div2;
  let t0;
  let div0;
  let t1;
  let div1;
  let button0;
  let pinButton_action;
  let t2;
  let t3;
  let button1;
  let trashIcon_action;
  let mounted;
  let dispose;
  let if_block0 = (
    /*displayFilename*/
    ctx[1] && create_if_block_1(ctx)
  );
  let if_block1 = (
    /*file*/
    ctx[0].parent != null && /*file*/
    ctx[0].parent.path !== "/" && create_if_block(ctx)
  );
  return {
    c() {
      div2 = element("div");
      if (if_block0)
        if_block0.c();
      t0 = space();
      div0 = element("div");
      t1 = space();
      div1 = element("div");
      button0 = element("button");
      t2 = space();
      if (if_block1)
        if_block1.c();
      t3 = space();
      button1 = element("button");
      attr(button0, "class", "clickable-icon svelte-liaymg");
      toggle_class(
        button0,
        "is-active",
        /*pinned*/
        ctx[3]
      );
      attr(button1, "class", "clickable-icon");
      attr(div1, "class", "card-info svelte-liaymg");
      attr(div2, "class", "card svelte-liaymg");
      attr(div2, "role", "link");
      attr(div2, "tabindex", "0");
      toggle_class(
        div2,
        "skip-transition",
        /*$skipNextTransition*/
        ctx[4]
      );
    },
    m(target, anchor) {
      insert(target, div2, anchor);
      if (if_block0)
        if_block0.m(div2, null);
      append(div2, t0);
      append(div2, div0);
      ctx[12](div0);
      append(div2, t1);
      append(div2, div1);
      append(div1, button0);
      append(div1, t2);
      if (if_block1)
        if_block1.m(div1, null);
      append(div1, t3);
      append(div1, button1);
      if (!mounted) {
        dispose = [
          action_destroyer(pinButton_action = /*pinButton*/
          ctx[8].call(null, button0)),
          listen(button0, "click", stop_propagation(
            /*togglePin*/
            ctx[5]
          )),
          action_destroyer(trashIcon_action = /*trashIcon*/
          ctx[9].call(null, button1)),
          listen(button1, "click", stop_propagation(
            /*trashFile*/
            ctx[6]
          )),
          listen(
            div2,
            "click",
            /*openFile*/
            ctx[7]
          ),
          listen(
            div2,
            "keydown",
            /*openFile*/
            ctx[7]
          )
        ];
        mounted = true;
      }
    },
    p(ctx2, [dirty]) {
      if (
        /*displayFilename*/
        ctx2[1]
      ) {
        if (if_block0) {
          if_block0.p(ctx2, dirty);
        } else {
          if_block0 = create_if_block_1(ctx2);
          if_block0.c();
          if_block0.m(div2, t0);
        }
      } else if (if_block0) {
        if_block0.d(1);
        if_block0 = null;
      }
      if (dirty & /*pinned*/
      8) {
        toggle_class(
          button0,
          "is-active",
          /*pinned*/
          ctx2[3]
        );
      }
      if (
        /*file*/
        ctx2[0].parent != null && /*file*/
        ctx2[0].parent.path !== "/"
      ) {
        if (if_block1) {
          if_block1.p(ctx2, dirty);
        } else {
          if_block1 = create_if_block(ctx2);
          if_block1.c();
          if_block1.m(div1, t3);
        }
      } else if (if_block1) {
        if_block1.d(1);
        if_block1 = null;
      }
      if (dirty & /*$skipNextTransition*/
      16) {
        toggle_class(
          div2,
          "skip-transition",
          /*$skipNextTransition*/
          ctx2[4]
        );
      }
    },
    i: noop,
    o: noop,
    d(detaching) {
      if (detaching) {
        detach(div2);
      }
      if (if_block0)
        if_block0.d();
      ctx[12](null);
      if (if_block1)
        if_block1.d();
      mounted = false;
      run_all(dispose);
    }
  };
}
function instance($$self, $$props, $$invalidate) {
  let $app;
  let $settings;
  let $view;
  let $skipNextTransition;
  component_subscribe($$self, app, ($$value) => $$invalidate(13, $app = $$value));
  component_subscribe($$self, settings, ($$value) => $$invalidate(11, $settings = $$value));
  component_subscribe($$self, view, ($$value) => $$invalidate(14, $view = $$value));
  component_subscribe($$self, skipNextTransition, ($$value) => $$invalidate(4, $skipNextTransition = $$value));
  let { file } = $$props;
  let displayFilename = true;
  let contentDiv;
  let pinned;
  function postProcessor(element2, context) {
    var _a, _b, _c, _d, _e;
    if (context.sourcePath !== file.path) {
      return;
    }
    if ($settings.displayTitle != "Both" /* Both */ && element2.children.length > 0 && element2.children[0].tagName === "H1" && element2.children[0].textContent) {
      if ($settings.displayTitle == "Title" /* Title */) {
        $$invalidate(1, displayFilename = false);
      } else if ($settings.displayTitle == "Filename" /* Filename */) {
        element2.children[0].remove();
      }
    }
    if (element2.children.length === 0) {
      return;
    }
    for (let i = 0; i < element2.children.length; i++) {
      if (element2.children[i].getElementsByClassName("internal-embed").length || element2.children[i].className.includes("block-language-dataview")) {
        console.log("Adding shadow to embed note");
        element2.children[i].appendChild(document.createElement("div")).className = "embed-shadow";
      }
    }
    let lastBlockIndex = 0, charCount = 0;
    do {
      charCount += ((_b = (_a = element2.children[lastBlockIndex]) === null || _a === void 0 ? void 0 : _a.textContent) === null || _b === void 0 ? void 0 : _b.length) || 0;
    } while (lastBlockIndex < element2.children.length && charCount < 200 && ++lastBlockIndex);
    for (let i = element2.children.length - 1; i > lastBlockIndex; i--) {
      (_c = element2.children[i]) === null || _c === void 0 ? void 0 : _c.remove();
    }
    if (charCount < 200) {
      return;
    }
    if (!element2.children[lastBlockIndex].lastChild || ((_d = element2.children[lastBlockIndex].lastChild) === null || _d === void 0 ? void 0 : _d.nodeType) !== Node.TEXT_NODE) {
      return;
    }
    const lastElText = (_e = element2.children[lastBlockIndex].lastChild) === null || _e === void 0 ? void 0 : _e.textContent;
    if (lastElText != null) {
      const cut = Math.min(50, 200 - (charCount - lastElText.length));
      element2.children[lastBlockIndex].lastChild.textContent = `${lastElText.slice(0, cut)} ...`;
    }
  }
  const renderFile = (el) => __awaiter(void 0, void 0, void 0, function* () {
    const content = yield file.vault.cachedRead(file);
    import_obsidian3.MarkdownPreviewRenderer.registerPostProcessor(postProcessor);
    yield import_obsidian3.MarkdownRenderer.render($app, content, el, file.path, $view);
    import_obsidian3.MarkdownPreviewRenderer.unregisterPostProcessor(postProcessor);
  });
  const togglePin = () => __awaiter(void 0, void 0, void 0, function* () {
    set_store_value(
      settings,
      $settings.pinnedFiles = pinned ? $settings.pinnedFiles.filter((f) => f !== file.path) : [...$settings.pinnedFiles, file.path],
      $settings
    );
  });
  const trashFile = () => __awaiter(void 0, void 0, void 0, function* () {
    yield file.vault.trash(file, true);
  });
  const openFile = () => __awaiter(void 0, void 0, void 0, function* () {
    return yield $app.workspace.getLeaf("tab").openFile(file);
  });
  const pinButton = (element2) => (0, import_obsidian3.setIcon)(element2, "pin");
  const trashIcon = (element2) => (0, import_obsidian3.setIcon)(element2, "trash");
  const folderIcon = (element2) => (0, import_obsidian3.setIcon)(element2, "folder");
  const dispatch = createEventDispatcher();
  onMount(() => __awaiter(void 0, void 0, void 0, function* () {
    yield renderFile(contentDiv);
    dispatch("loaded");
  }));
  function div0_binding($$value) {
    binding_callbacks[$$value ? "unshift" : "push"](() => {
      contentDiv = $$value;
      $$invalidate(2, contentDiv);
    });
  }
  $$self.$$set = ($$props2) => {
    if ("file" in $$props2)
      $$invalidate(0, file = $$props2.file);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty & /*$settings, file*/
    2049) {
      $:
        $$invalidate(3, pinned = $settings.pinnedFiles.includes(file.path));
    }
  };
  return [
    file,
    displayFilename,
    contentDiv,
    pinned,
    $skipNextTransition,
    togglePin,
    trashFile,
    openFile,
    pinButton,
    trashIcon,
    folderIcon,
    $settings,
    div0_binding
  ];
}
var Card = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance, create_fragment, safe_not_equal, { file: 0 }, add_css);
  }
};
var Card_default = Card;

// components/Root.svelte
function add_css2(target) {
  append_styles(target, "svelte-1y01xi6", ".action-bar.svelte-1y01xi6.svelte-1y01xi6{position:relative;display:flex;flex-direction:row-reverse;gap:var(--size-4-5);margin-bottom:var(--size-4-5);flex-wrap:wrap;@media screen and (min-width: 700px) {\n      flex-wrap: nowrap;\n    }}.action-bar.svelte-1y01xi6 .action-bar__tags.svelte-1y01xi6{flex-grow:1;-ms-overflow-style:none;scrollbar-width:none;white-space:nowrap;overflow:auto;&::-webkit-scrollbar {\n      display: none;\n    };@media screen and (min-width: 700px) {\n      overflow: hidden;\n\n      & .action-bar__tags__list {\n        transition: box-shadow 0.3s;\n      }\n\n      &:hover .action-bar__tags__list {\n        position: absolute;\n        top: 0;\n        left: 0;\n        right: 0;\n\n        white-space: normal;\n        z-index: var(--layer-popover);\n        background-color: var(--background-primary);\n        padding-bottom: var(--size-4-2);\n        box-shadow: 0 0 var(--size-2-3) var(--size-2-3)\n          var(--background-primary);\n\n        & button.action-bar__tag {\n          margin-bottom: var(--size-2-2);\n        }\n      }\n    }}.action-bar.svelte-1y01xi6 button.action-bar__tag.svelte-1y01xi6{font-size:var(--tag-size);color:var(--tag-color);text-decoration:var(--tag-decoration);background-color:var(--tag-background);border:var(--tag-border-width) solid var(--tag-border-color);padding:var(--tag-padding-y) var(--tag-padding-x);border-radius:var(--tag-radius);font-weight:var(--tag-weight);margin-right:var(--size-2-2);&:hover {\n      color: var(--tag-color-hover);\n      text-decoration: var(--tag-decoration-hover);\n      background-color: var(--tag-background-hover);\n      border-color: var(--tag-border-color-hover);\n    }}.action-bar.svelte-1y01xi6 .action-bar__search.svelte-1y01xi6{flex-grow:1;@media screen and (min-width: 700px) {\n      min-width: 300px;\n      max-width: 300px;\n      box-shadow: calc(0px - var(--size-4-5)) 0 var(--size-2-3) var(--size-2-3)\n        var(--background-primary);\n    }}.cards-container.svelte-1y01xi6.svelte-1y01xi6{position:relative;container-type:inline-size}.cards-container.svelte-1y01xi6 *{--card-padding:var(--size-4-3);--card-gutter:var(--size-4-5)}");
}
function get_each_context(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[18] = list[i];
  return child_ctx;
}
function get_each_context_1(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[21] = list[i];
  return child_ctx;
}
function create_each_block_1(ctx) {
  let button;
  let t_value = (
    /*tag*/
    ctx[21] + ""
  );
  let t;
  let mounted;
  let dispose;
  function click_handler() {
    return (
      /*click_handler*/
      ctx[10](
        /*tag*/
        ctx[21]
      )
    );
  }
  return {
    c() {
      button = element("button");
      t = text(t_value);
      attr(button, "class", "action-bar__tag svelte-1y01xi6");
    },
    m(target, anchor) {
      insert(target, button, anchor);
      append(button, t);
      if (!mounted) {
        dispose = listen(button, "click", click_handler);
        mounted = true;
      }
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      if (dirty & /*$tags*/
      32 && t_value !== (t_value = /*tag*/
      ctx[21] + ""))
        set_data(t, t_value);
    },
    d(detaching) {
      if (detaching) {
        detach(button);
      }
      mounted = false;
      dispose();
    }
  };
}
function create_each_block(key_1, ctx) {
  let first;
  let card;
  let current;
  card = new Card_default({ props: { file: (
    /*file*/
    ctx[18]
  ) } });
  card.$on(
    "loaded",
    /*loaded_handler*/
    ctx[12]
  );
  return {
    key: key_1,
    first: null,
    c() {
      first = empty();
      create_component(card.$$.fragment);
      this.first = first;
    },
    m(target, anchor) {
      insert(target, first, anchor);
      mount_component(card, target, anchor);
      current = true;
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      const card_changes = {};
      if (dirty & /*$displayedFiles*/
      64)
        card_changes.file = /*file*/
        ctx[18];
      card.$set(card_changes);
    },
    i(local) {
      if (current)
        return;
      transition_in(card.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(card.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching) {
        detach(first);
      }
      destroy_component(card, detaching);
    }
  };
}
function create_fragment2(ctx) {
  let div3;
  let button;
  let sortIcon_action;
  let t0;
  let div0;
  let searchInput_action;
  let t1;
  let div2;
  let div1;
  let t2;
  let div4;
  let each_blocks = [];
  let each1_lookup = /* @__PURE__ */ new Map();
  let current;
  let mounted;
  let dispose;
  let each_value_1 = ensure_array_like(
    /*$tags*/
    ctx[5]
  );
  let each_blocks_1 = [];
  for (let i = 0; i < each_value_1.length; i += 1) {
    each_blocks_1[i] = create_each_block_1(get_each_context_1(ctx, each_value_1, i));
  }
  let each_value = ensure_array_like(
    /*$displayedFiles*/
    ctx[6]
  );
  const get_key = (ctx2) => (
    /*file*/
    ctx2[18].path + /*file*/
    ctx2[18].stat.mtime
  );
  for (let i = 0; i < each_value.length; i += 1) {
    let child_ctx = get_each_context(ctx, each_value, i);
    let key = get_key(child_ctx);
    each1_lookup.set(key, each_blocks[i] = create_each_block(key, child_ctx));
  }
  return {
    c() {
      div3 = element("div");
      button = element("button");
      t0 = space();
      div0 = element("div");
      t1 = space();
      div2 = element("div");
      div1 = element("div");
      for (let i = 0; i < each_blocks_1.length; i += 1) {
        each_blocks_1[i].c();
      }
      t2 = space();
      div4 = element("div");
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      attr(button, "class", "clickable-icon sort-button");
      attr(div0, "class", "action-bar__search svelte-1y01xi6");
      attr(div1, "class", "action-bar__tags__list");
      attr(div2, "class", "action-bar__tags svelte-1y01xi6");
      attr(div3, "class", "action-bar svelte-1y01xi6");
      attr(div4, "class", "cards-container svelte-1y01xi6");
      set_style(
        div4,
        "--columns",
        /*columns*/
        ctx[3]
      );
    },
    m(target, anchor) {
      insert(target, div3, anchor);
      append(div3, button);
      append(div3, t0);
      append(div3, div0);
      append(div3, t1);
      append(div3, div2);
      append(div2, div1);
      for (let i = 0; i < each_blocks_1.length; i += 1) {
        if (each_blocks_1[i]) {
          each_blocks_1[i].m(div1, null);
        }
      }
      ctx[11](div3);
      insert(target, t2, anchor);
      insert(target, div4, anchor);
      for (let i = 0; i < each_blocks.length; i += 1) {
        if (each_blocks[i]) {
          each_blocks[i].m(div4, null);
        }
      }
      ctx[13](div4);
      current = true;
      if (!mounted) {
        dispose = [
          action_destroyer(sortIcon_action = /*sortIcon*/
          ctx[7].call(null, button)),
          listen(
            button,
            "click",
            /*sortMenu*/
            ctx[9]
          ),
          action_destroyer(searchInput_action = /*searchInput*/
          ctx[8].call(null, div0))
        ];
        mounted = true;
      }
    },
    p(ctx2, [dirty]) {
      if (dirty & /*$searchQuery, $tags*/
      48) {
        each_value_1 = ensure_array_like(
          /*$tags*/
          ctx2[5]
        );
        let i;
        for (i = 0; i < each_value_1.length; i += 1) {
          const child_ctx = get_each_context_1(ctx2, each_value_1, i);
          if (each_blocks_1[i]) {
            each_blocks_1[i].p(child_ctx, dirty);
          } else {
            each_blocks_1[i] = create_each_block_1(child_ctx);
            each_blocks_1[i].c();
            each_blocks_1[i].m(div1, null);
          }
        }
        for (; i < each_blocks_1.length; i += 1) {
          each_blocks_1[i].d(1);
        }
        each_blocks_1.length = each_value_1.length;
      }
      if (dirty & /*$displayedFiles, notesGrid*/
      65) {
        each_value = ensure_array_like(
          /*$displayedFiles*/
          ctx2[6]
        );
        group_outros();
        each_blocks = update_keyed_each(each_blocks, dirty, get_key, 1, ctx2, each_value, each1_lookup, div4, outro_and_destroy_block, create_each_block, null, get_each_context);
        check_outros();
      }
      if (dirty & /*columns*/
      8) {
        set_style(
          div4,
          "--columns",
          /*columns*/
          ctx2[3]
        );
      }
    },
    i(local) {
      if (current)
        return;
      for (let i = 0; i < each_value.length; i += 1) {
        transition_in(each_blocks[i]);
      }
      current = true;
    },
    o(local) {
      for (let i = 0; i < each_blocks.length; i += 1) {
        transition_out(each_blocks[i]);
      }
      current = false;
    },
    d(detaching) {
      if (detaching) {
        detach(div3);
        detach(t2);
        detach(div4);
      }
      destroy_each(each_blocks_1, detaching);
      ctx[11](null);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].d();
      }
      ctx[13](null);
      mounted = false;
      run_all(dispose);
    }
  };
}
function instance2($$self, $$props, $$invalidate) {
  let $skipNextTransition;
  let $viewIsVisible;
  let $settings;
  let $sort;
  let $searchQuery;
  let $tags;
  let $displayedFiles;
  component_subscribe($$self, skipNextTransition, ($$value) => $$invalidate(14, $skipNextTransition = $$value));
  component_subscribe($$self, viewIsVisible, ($$value) => $$invalidate(15, $viewIsVisible = $$value));
  component_subscribe($$self, settings, ($$value) => $$invalidate(16, $settings = $$value));
  component_subscribe($$self, sort, ($$value) => $$invalidate(17, $sort = $$value));
  component_subscribe($$self, searchQuery, ($$value) => $$invalidate(4, $searchQuery = $$value));
  component_subscribe($$self, tags, ($$value) => $$invalidate(5, $tags = $$value));
  component_subscribe($$self, displayedFiles, ($$value) => $$invalidate(6, $displayedFiles = $$value));
  let notesGrid;
  let viewContent;
  let cardsContainer;
  let columns;
  const sortIcon = (element2) => {
    (0, import_obsidian4.setIcon)(element2, "arrow-down-wide-narrow");
  };
  const searchInput = (element2) => {
    const searchInput2 = new import_obsidian4.SearchComponent(element2);
    searchInput2.onChange((value) => {
      set_store_value(searchQuery, $searchQuery = value, $searchQuery);
    });
    searchQuery.subscribe((value) => {
      searchInput2.inputEl.value = value;
    });
  };
  function sortMenu(event) {
    const sortMenu2 = new import_obsidian4.Menu();
    sortMenu2.addItem((item) => {
      item.setTitle("Last created");
      item.setChecked($sort == "ctime" /* Created */);
      item.onClick(() => __awaiter(this, void 0, void 0, function* () {
        set_store_value(sort, $sort = "ctime" /* Created */, $sort);
      }));
    });
    sortMenu2.addItem((item) => {
      item.setTitle("Last modified");
      item.setChecked($sort == "mtime" /* Modified */);
      item.onClick(() => __awaiter(this, void 0, void 0, function* () {
        set_store_value(sort, $sort = "mtime" /* Modified */, $sort);
      }));
    });
    sortMenu2.showAtMouseEvent(event);
  }
  onMount(() => {
    $$invalidate(3, columns = Math.floor(viewContent.clientWidth / $settings.minCardWidth));
    $$invalidate(0, notesGrid = new MiniMasonry({
      container: cardsContainer,
      baseWidth: $settings.minCardWidth,
      gutter: 20,
      surroundingGutter: false,
      ultimateGutter: 20
    }));
    notesGrid.layout();
    return () => {
      notesGrid.destroy();
    };
  });
  afterUpdate((0, import_obsidian4.debounce)(() => __awaiter(void 0, void 0, void 0, function* () {
    if (!$viewIsVisible) {
      set_store_value(skipNextTransition, $skipNextTransition = true, $skipNextTransition);
      return;
    }
    notesGrid.layout();
    set_store_value(skipNextTransition, $skipNextTransition = false, $skipNextTransition);
  })));
  const click_handler = (tag) => set_store_value(searchQuery, $searchQuery = tag, $searchQuery);
  function div3_binding($$value) {
    binding_callbacks[$$value ? "unshift" : "push"](() => {
      viewContent = $$value;
      $$invalidate(1, viewContent);
    });
  }
  const loaded_handler = () => notesGrid.layout();
  function div4_binding($$value) {
    binding_callbacks[$$value ? "unshift" : "push"](() => {
      cardsContainer = $$value;
      $$invalidate(2, cardsContainer);
    });
  }
  return [
    notesGrid,
    viewContent,
    cardsContainer,
    columns,
    $searchQuery,
    $tags,
    $displayedFiles,
    sortIcon,
    searchInput,
    sortMenu,
    click_handler,
    div3_binding,
    loaded_handler,
    div4_binding
  ];
}
var Root = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance2, create_fragment2, safe_not_equal, {}, add_css2);
  }
};
var Root_default = Root;

// view.ts
var VIEW_TYPE = "cards-view";
var CardsViewPluginView = class extends import_obsidian5.ItemView {
  constructor(settings2, leaf) {
    super(leaf);
    this.settings = settings2;
  }
  getViewType() {
    return VIEW_TYPE;
  }
  getDisplayText() {
    return "Cards View";
  }
  async onOpen() {
    const viewContent = this.containerEl.children[1];
    store_default.view.set(this);
    store_default.files.set(this.app.vault.getMarkdownFiles());
    this.registerEvent(
      this.app.vault.on("create", async (file) => {
        if (!this.app.workspace.layoutReady) {
          return;
        }
        if (file instanceof import_obsidian5.TFile && file.extension === "md") {
          store_default.files.update((files2) => files2 == null ? void 0 : files2.concat(file));
        }
      })
    );
    this.registerEvent(
      this.app.vault.on("delete", async (file) => {
        if (file instanceof import_obsidian5.TFile && file.extension === "md") {
          store_default.files.update(
            (files2) => files2 == null ? void 0 : files2.filter((f) => f.path !== file.path)
          );
        }
      })
    );
    this.registerEvent(
      this.app.vault.on("modify", async (file) => {
        if (file instanceof import_obsidian5.TFile && file.extension === "md") {
          store_default.files.update(
            (files2) => files2 == null ? void 0 : files2.map((f) => f.path === file.path ? file : f)
          );
        }
      })
    );
    this.registerEvent(
      this.app.vault.on(
        "rename",
        async (file, oldPath) => {
          if (file instanceof import_obsidian5.TFile && file.extension === "md") {
            store_default.files.update(
              (files2) => files2 == null ? void 0 : files2.map((f) => f.path === oldPath ? file : f)
            );
          }
        }
      )
    );
    this.svelteRoot = new Root_default({
      target: viewContent
    });
    viewContent.addEventListener("scroll", async () => {
      if (viewContent.scrollTop + viewContent.clientHeight > viewContent.scrollHeight - 500) {
        store_default.skipNextTransition.set(true);
        store_default.displayedCount.set(get_store_value(store_default.displayedFiles).length + 50);
      }
    });
    this.app.workspace.on("active-leaf-change", () => {
      var _a;
      const rootLeaf = this.app.workspace.getMostRecentLeaf(
        this.app.workspace.rootSplit
      );
      store_default.viewIsVisible.set(((_a = rootLeaf == null ? void 0 : rootLeaf.view) == null ? void 0 : _a.getViewType()) === VIEW_TYPE);
    });
  }
  async onClose() {
    store_default.viewIsVisible.set(false);
    store_default.searchQuery.set("");
    store_default.displayedCount.set(50);
    store_default.sort.set("mtime" /* Modified */);
  }
};

// main.ts
var CardsViewPlugin2 = class extends import_obsidian6.Plugin {
  constructor() {
    super(...arguments);
    this.settings = Object.assign({}, DEFAULT_SETTINGS);
  }
  async onload() {
    this.settings = Object.assign(this.settings, await this.loadData());
    store_default.settings.subscribe(async () => await this.saveSettings());
    store_default.app.set(this.app);
    store_default.settings.set(this.settings);
    store_default.appCache.set(this.app.metadataCache);
    this.registerEvent(
      this.app.metadataCache.on(
        "resolved",
        async () => store_default.appCache.update(() => this.app.metadataCache)
      )
    );
    this.addSettingTab(new CardsViewSettingsTab(this.app, this));
    this.addRibbonIcon("align-start-horizontal", "Card view", () => {
      this.activateView();
    });
    this.addCommand({
      id: "cards-view-plugin",
      name: "Open card view",
      callback: () => {
        this.activateView();
      }
    });
    this.registerView(
      VIEW_TYPE,
      (leaf) => new CardsViewPluginView(this.settings, leaf)
    );
    this.app.workspace.onLayoutReady(() => {
      if (this.settings.launchOnStart) {
        this.activateView();
      }
    });
  }
  onunload() {
  }
  async activateView() {
    const { workspace } = this.app;
    let leaf;
    const leaves = workspace.getLeavesOfType(VIEW_TYPE);
    if (leaves.length) {
      leaf = leaves[0];
    } else {
      leaf = workspace.getLeaf("tab");
    }
    await leaf.setViewState({ type: VIEW_TYPE, active: true });
    store_default.viewIsVisible.set(true);
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
};

/* nosourcemap */